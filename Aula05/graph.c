#include <string.h>
#include <stdio.h>
#include <stdlib.h>

void search(int **image, int minRow, int maxRow){

}

int fileHeight(){
    int height = 0;
    return height;
}
int fileWidth(){
    int width = 0;
    return width;
}

int main(){
    int **image;
    image = (int**)malloc(sizeof(int*)*fileHeight());
    *image = (int*)malloc(sizeof(int)*fileHeight());
}